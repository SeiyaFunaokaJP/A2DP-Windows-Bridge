# tools/emu Python environment (installed by setup_env.py into tools/emu/.venv).
# Development / testing only: nothing here is linked into or shipped with A2DPWB.
# Everything is pinned so the environment is reproducible; licenses are listed
# in THIRD_PARTY_LICENSES.md section 13. Update both together, and
# tools/linux_sink/requirements.txt (the same pins).

# Virtual Bluetooth controllers + A2DP sink (emu_sink.py extends its controller)
bumble==0.0.234

# Dependencies of bumble 0.0.234
cffi==2.1.1
click==8.5.0
cryptography==50.0.1
importlib_resources==7.1.0
libusb-package==1.0.26.4
libusb1==3.4.0
platformdirs==4.11.12
prompt_toolkit==3.0.53
pycparser==3.0
pyee==14.0.0
pyserial==3.5
pyserial-asyncio==0.6
pyusb==1.3.1
typing_extensions==4.16.0
wcwidth==0.9.1
websockets==17.1
